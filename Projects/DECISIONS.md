# Decisions

## 2026-05-25: Project Organisation

- Use `/home/cef-admin/.openclaw/workspace/second-brain/Projects` as the local project folder for project collateral.
- Use `/home/cef-admin/projects-source-code` as the source-code root.
- Each buildable project gets a separate folder under `/home/cef-admin/projects-source-code` and should map to its own GitHub repository.
- Keep OpenClaw runtime memory/config in `/home/cef-admin/.openclaw/workspace`.
- Archive old prototypes under `/home/cef-admin/projects/archive` instead of deleting them.
